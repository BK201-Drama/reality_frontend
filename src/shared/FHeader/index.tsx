import { Layout } from 'antd';

const { Header } = Layout;

const FHeader = () => {
  return (
    <Header className="bg-[#fff]">
      <div className="" />
    </Header>
  )
}

export default FHeader